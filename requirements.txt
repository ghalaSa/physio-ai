# Core
numpy>=1.26
pandas>=2.2
scipy>=1.12
scikit-learn>=1.4
matplotlib>=3.8
pyyaml>=6.0
tqdm>=4.66
requests>=2.31

# Computer vision
opencv-python-headless>=4.9
# mediapipe 1.0.x aborts on macOS (Metal helper "Service is unavailable" in the pose graph);
# 0.10.35 works on macOS, 1.0.1 was used for the Linux extraction on the VM (same model file).
mediapipe>=0.10.14,<1.0; sys_platform == "darwin"
mediapipe>=0.10.14; sys_platform != "darwin"

# Deep learning
torch>=2.2

# Backend / frontend / storage
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
sqlalchemy>=2.0
streamlit>=1.35
plotly>=5.20

# LLM (provider-independent interface; Anthropic is the reference implementation)
anthropic>=0.40

# Testing
pytest>=8.0
httpx>=0.27
